language: bash

# See https://discourse.gohugo.io/t/solved-hugo-v44-extended-and-relocation-errors-on-travis/13029/4
before_install:
  # This workaround is required to avoid libstdc++ errors while running "extended" hugo with SASS support.
  - wget -q -O libstdc++6 http://security.ubuntu.com/ubuntu/pool/main/g/gcc-5/libstdc++6_5.4.0-6ubuntu1~16.04.10_amd64.deb
  - sudo dpkg --force-all -i libstdc++6
install:
  - wget -q -O hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.48/hugo_extended_0.48_Linux-64bit.deb
  - sudo dpkg -i hugo.deb

script:
  - hugo

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: public
  fqdn: hilariofor25.com
  verbose: true
  target-branch: test-gh-pages
  on:
    branch: master

# TODO: make travis commit resources branch back to master for faster builds when images are added via forestry
# after_success: |
#   git add resources
#   git -c user.name='travis' -c user.email='travis' commit -m "Update resources dir for caching; skip ci"
#   git push origin
